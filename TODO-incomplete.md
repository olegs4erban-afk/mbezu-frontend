# TODO — что осталось незавершённым (требует данных/решений владельца)

> Сборка зелёная и полная. Ниже — пункты, которые НЕЛЬЗЯ закрыть автономно
> (нужны реальные ID/данные или решение владельца). Не выдумывать значения.

## 1. Реальные ID аналитики (плейсхолдеры в `src/common/analytics.ts`)
Скрипты трекеров НЕ загружаются, пока ID остаются плейсхолдерами (защита от мусорных хитов).
Заменить перед/при деплое:
- `yandexMetrika`: `XXXXXXXX` → номер счётчика Я.Метрики
- `ga4`: `G-XXXXXXXXXX` → GA4 Measurement ID
- `vkPixel`: `VK-RTRG-XXXXXX-XXXXX` → VK Ads/Top пиксель (+ финальный сниппет VK в `initVkPixel`)

## 2. Работы без фото (placeholder-градиент вместо снимка)
- **MN-03 «Шторм»** — фото нет в `assets/works/`. В каталоге/карточке рендерится палитра-плейсхолдер.
  Per-artwork страница и Product-JSON-LD генерируются (статус available), но без `og:image`.
  → Прислать фото `mn-03.jpg` (+ `@md`/`@sm`) или подтвердить, что работа снята с продажи.

## 3. Работы с провизорными ценой/размером (⚠ в исходнике помечены «уточнить»)
Фото есть, но цена и/или габариты предварительные — НЕ публиковать в Tilda-контейнерах до подтверждения
(см. `painting-containers.md` → раздел «отложены»):
- **ST-08 «Обидуш»** — размер 30×30 и цена 75 000 ₽ помечены «уточнить».
- **TD-01 «Sands»** — диаметр 40 и цена 85 000 ₽ помечены «уточнить».
- **TD-02 «Hibiscus»** — диаметр 50 и цена 130 000 ₽ помечены «уточнить».

## 4. AR-ассеты (3D)
`assets/ar/<id>.glb` / `.usdz` отсутствуют (есть только README). `ArViewer` показывает placeholder,
`@google/model-viewer` (ленивый чанк) не грузится. → Добавить .glb/.usdz, выставить `ready:true` в `src/ar/ar.tsx`.

## 5. Деплой-привязки (см. DEPLOY.md, Phase 6)
- Cloudflare Pages проект + custom domain `cdn.mbezu.ru` (CNAME) — действие владельца.
- GitHub remote: `gh` не установлен на машине → репозиторий пока только локальный.

## 6. Остаточные перф-оптимизации (Sprint 3, не блокеры — Lighthouse-цели достигнуты)
- **WebP/AVIF для фото работ** (Lighthouse `modern-image-formats`, ~115–355 KiB экономии): нужен
  build-пайплайн (`sharp`/`vite-imagetools`) — `sharp` не установлен в текущем окружении.
  Сейчас отдаём оптимизированный JPEG + адаптивный `srcSet`. → добавить webp-вариант + `<picture>`/`srcset type`.
- **Шрифты (home LCP/CLS)**: Google Fonts грузятся внешне; для лучших LCP/CLS — самохостинг woff2
  + `preload` или тюнинг `font-display`. Это UX/инфра-решение (поведение FOUT/первый кадр) → за владельцем.

